## Packages
recharts | Dashboard analytics charts
framer-motion | Smooth animations and page transitions
date-fns | Date formatting for tables and metadata
react-dropzone | Drag and drop file uploads

## Notes
File uploads use standard FormData to /api/files/upload for server-side encryption
Auth is handled via Replit Auth (useAuth hook)
Public download links are at /s/:token
